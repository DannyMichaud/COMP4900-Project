#include <stdio.h>
#include <unistd.h>
#include <sys/neutrino.h>
#include <stdlib.h>

int main(int argc, char **argv) {
    printf("TEST!\n");
    //printf("I was passed in %d args, let's see what 0, 1, 2 are %s %s %s", argc, argv[0], argv[1], argv[2]);
}
